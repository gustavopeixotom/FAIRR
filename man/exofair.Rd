% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exofair.R
\name{exofair}
\alias{exofair}
\title{Estimate FAIR_G(K) Models with Exogenous Shocks}
\usage{
exofair(
  Y,
  Z,
  H,
  K = 1,
  priorvec = c(1, 1, 1),
  detrend = 0,
  asym = FALSE,
  sdpar = 20,
  cores = NA,
  chains = 4,
  iter = 2000,
  adapt_delta = 0.95,
  max_treedepth = 12,
  timeout = 180
)
}
\arguments{
\item{Y}{A numeric vector or matrix. The dependent variable.}

\item{Z}{A numeric vector or matrix. The exogenous shock variable.}

\item{H}{Integer. Specifies the horizon of the IRF.}

\item{K}{Integer (1 or 2). Specifies the number of basis functions used.}

\item{priorvec}{A numeric vector of length 3*K containing priors.
\itemize{
\item If \code{K=1}: \code{c(a, b, c)}
\item If \code{K=2}: \code{c(a1, b1, c1, a2, b2, c2)}
}}

\item{detrend}{Integer (0, 1, or 2). Controls data transformation:
\itemize{
\item \code{0}: No trend removed.
\item \code{1}: Log-difference (\code{diff(log(Y))}).
\item \code{2}: Linear time trend removed (residuals of \code{lm(Y ~ time)}).
}}

\item{asym}{Logical. If \code{TRUE}, fits separate IRFs for positive and negative shocks. If \code{FALSE}, fits a single symmetric IRF.}

\item{sdpar}{Numeric. Standard deviation of the Normal prior for parameters b and c.}

\item{cores}{Integer. Number of CPU cores for Stan. If greater than \code{chains}, defaults to \code{chains}.}

\item{chains}{Integer. Number of Markov chains (see \code{\link[rstan:stan]{rstan::stan}}).}

\item{iter}{Integer. Number of iterations per chain (see \code{\link[rstan:stan]{rstan::stan}}).}

\item{adapt_delta}{Numeric. Target average acceptance probability (see \code{\link[rstan:stan]{rstan::stan}}).}

\item{max_treedepth}{Integer. Maximum tree depth (see \code{\link[rstan:stan]{rstan::stan}}).}

\item{timeout}{Numeric. Maximum seconds allowed for the Stan run.}
}
\value{
A list containing:
\describe{
\item{fair}{Posterior draws of the FAIR coefficients and corresponding IRF.}
\item{stan}{Summary of the Stan model fit.}
\item{data}{The processed data used for estimation.}
}
}
\description{
Function Approximation of Impulse Responses (FAIR) using Gaussian basis functions using shocks that are already considered exogenous.
}
